================================================================================
                                样例使用说明
================================================================================
版本历史
日期        版本    负责人         IAR     MDK     GCC     描述
2019-07-04  1.0     Yangjp         7.70    5.16    8.3.1   first version
================================================================================
平台说明
================================================================================
GCC工程，由Eclipse IDE外挂GNU-ARM Toolchain，再结合pyOCD GDB Server实现工程的编译、
链接和调试。在用Eclipse导入工程后，请将xxxx_PyOCDDebug中pyocd-gdbserver和SVD文件
设置为正确的路径；请将xxxx_PyOCDDownload中pyocd设置为正确的路径。注意，这些路径不
能包含非英文字符。


功能描述
================================================================================
本样例展示TimerA模块PWM输出及外部中断触发Timer启动/停止功能。

说明：
本样例设置TimerA为三角波计数模式，TimerA单元1的通道1配置为PWM输出模式并开启缓存
功能，通过按键SW2启动/停止TimerA，通过按键SW1改变输出占空比，将示波器连接到P73
（通道1）观察输出波形。

================================================================================
测试环境
================================================================================
测试用板:
---------------------
STK_HC32M120_LQFP48_050_V11

辅助工具:
---------------------
示波器

辅助软件:
---------------------
无

================================================================================
使用步骤
================================================================================
1）打开工程并重新编译；
2）启动IDE的下载程序功能，关闭IDE下载界面；
3）按键SW2启动TimerA的PWM输出功能，观察LED_R常亮，PWM频率为50Hz；
4）将示波器连接到P73观察输出波形，反复触发按键SW1改变输出占空比；
5）观察示波器波形变化，再次按下SW2暂停TimerA，停止PWM输出；
6）观察示波器没有波形输出，可反复执行3-5步骤。

================================================================================
注意
================================================================================
无

================================================================================
